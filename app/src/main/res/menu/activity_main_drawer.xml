<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/menu_home" />
    </group>

    <item android:title="Personal Health">
        <menu>
            <item
                android:id="@+id/nav_record_pressure"
                android:icon="@drawable/icons8_heart_monitor_24px"
                android:title="@string/menu_record_pressure" />
            <item
                android:id="@+id/nav_view_record"
                android:icon="@drawable/ic_assignment_black_24dp"
                android:title="@string/menu_view_record" />
        </menu>
    </item>

</menu>
